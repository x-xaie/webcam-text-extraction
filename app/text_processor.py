# app/text_processor.py
def process_text(raw_text):
    """Clean and format the extracted text."""
    # Example: strip unwanted spaces or newlines
    processed_text = raw_text.strip()
    return processed_text
